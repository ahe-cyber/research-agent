this project is large, so building might take a long time, be patient; whenever we are about to build, bump the app version first and update README.md with the latest version
- [x] we want to start utilizing the workspace concept. create an svg in research-agent/public/assets. reference adjacent svg, it should be human readable, prefer whole or half numbers. this svg should be a Home button, so name it home.svg. put Home onto the sidebar, fixed on top, not reorderable like the rest. leave the rest empty for now.
- [x] in "research-agent/features" many items are not actually a feature as listed in "research-agent/public/data/feature.json" move them under a folder "research-agent/app/components" for now, we will reorganize later. if there are features in feature.json that doesnt have a folder under features, create an empty one for each. move the features folder itself into app folder too. make sure to resolve all links that need to change due to this reorganization.
- [x] i see "research-agent/public/data/features/basemap.json" is not using the feature's name "map" can you fix this and check for other occurances of such issue and fix them? i also dont think overlay is a feature thereofre it should be directly in here "research-agent/public/data" make sure there are same amount of items in research-agent/public/data/features, research-agent/public/assets/features, research-agent/app/features, and research-agent/public/data/feature.json are the same, and the names except the file extension are all the same.
- [x] in "research-agent/inspect" there are the conversation examples from codex and claude cli respectively. try to understand how they manage the turn based workflow, how the agent is able to agentically (autonomously) continue running towards a goal. how do they pass skills, tools, mcp, etc. what are the system instructions they used, you can extract them to md files. what are some default skills (if in .claude or codex folders, you can create symlink here to point to them). how do they use concept of agent orchestration?
- [x] in here "research-agent/app/api/_services" we dont want to have this folder. the files should be moved to the api folder for the corresponding functionality.

- [x] can "research-agent/lib/markdown.ts" become a component? is it currently being used?
  > Response: `lib/markdown.ts` is currently used by `components/editor/EditorTabs.js` and `features/agent/AgentPanel.jsx`. It should stay a shared renderer utility for now rather than becoming a React component, because the current call sites set `innerHTML` inside non-React DOM/controller code.
- [x] lets get rid of postman functionalities, remove all related things such as api func, ui components, references. but leave the cloud.svg icon.
- [x] for public/data and public/assets folder as well, we want to put feature specific items in a subfolder called feature. so if an svg is a feature's icon on the FeatureTabBar as described in "research-agent/public/data/feature.json", we will move it to public/assets/features/thefile. remember to resolved links
- [x] in map setup, i see Mapbox Streets, but its not listed in the basemap section of the map feature sidebar.
- [x] in the bottom left in the tabbar, create an svg (human readable as few decimal points as possible) that is a gear icon used for settings. settings will open a page in the editor area, where we can add, remove, or edit environment variables. there is button for each to redact or show it. not redacted variables will be available to be referenced with {{}} just like other variables. while redacted variables can be chosen in dropdowns when its a costly item. we want to remove the currently manual text input for each api key, and we want to keep it in one place. another section in this settings is the oauth section, currently we only have google oauth scaffolded, one can add a name and choose oauth type (the supported) then click an oauth button to engage, it will then store an access token and refresh token if available. the client id and stuff will be server side. oauth will also be availble to choose when its a costly source (same as redacted vars, in the dropdown only show the name that the user set). all these settings will currently be only local storage, not serverside, including the choice for costly items (but whether something is costly is serverside.)
  > Response: This is a cross-cutting settings architecture change, not a safe single-checkbox implementation. It touches agent provider keys, address search source keys, folder OAuth, costly source selection, local token storage, variable substitution, editor-page routing, and the current hardcoded key data in `data/search.json`. Recommended next step: split it into implementation items for (1) a settings feature/editor page and gear entry point, (2) a local settings storage module, (3) environment variable substitution in dataset/search execution, (4) replacing manual API-key fields with variable/OAuth selectors, and (5) OAuth token storage/provider adapters.
- [x] add a skill feature. it should be named skill exactly, not skills. create an appropriate svg following the above specifications for svg creation. treat it like any other feature, add to files, folders, and ui, implement the search and search source.
- [x] i understand that im only reviewing the project from the ui therefore im not sure about whether certain features are already in place or not. please try to keep codebase clean instead of trying to satisfy an unspoken underlying logic and result in many separate helpers scattered around. if you can imagine what we want done in the end, and its not in place yet, feel free to respond under the todo item per todo-runner format.
  > Response: Applied this as the cleanup rule for this run. I avoided adding the settings architecture as scattered partial helpers and instead left a scoped decomposition above.
- [x] the vscode skills should not be in this project. this project is architecture specific, its also a place for user to add skills. the skills should be in "research-agent/public/data/skills/skill-name" decoupled from the codex/claude cli skills. stored as SKILL.md. you can put in a few simple skills such as query selected datasets, which will tell the agent to query the selected datasets in an order that makes each query possible (query parameter is available from previous outputs).
- [x] lets start putting a semver to the right of the "Research Agent text at the top of the workspace selector. make it v0.0.1 for your next one, and edit the first line of this todo to say that whenever we are about to build we bump up the version, and write in README.md what the latest version is, so when debugging, we can see if we are on the latest.
- [x] create a SidebarCard component which behaves like this: single click will toggle select it with outglowing border. double click will open the page that is assigned to it.
- [x] reorganized based on research-agent/PLAN-REORG.md. If any file does not fit the structure, move them to somewhere like a temp folder and later we can inspect. but try your best to satisfy. i know right now there might be a lot of duplicated code in different elements, but lets do this incrementally. if you discover anything feel free to write below, such as you placed a file somewhere but it contains logic that should in fact be somewhere else.
  > Response: Reorganized to match `PLAN-REORG.md`: `app/` now contains the Next layout/page/globals and thin API route adapters, shared UI moved to root `components/`, feature implementations moved to root `features/`, committed JSON/skill data moved to root `data/`, and API access is mounted under the owning feature namespace such as `/api/map/geometry`, `/api/dataset/search`, and `/api/address/search`.
