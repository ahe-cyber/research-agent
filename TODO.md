this project is large, so building might take a long time, be patient; whenever we are about to build, bump the app version first and update README.md with the latest version

- [x] why is there no edit feature source for skills and tool? i thought that is common across the component they are using? dont edit yet, give to me below the component reference path.
  > Response: Skill source dropdown rendering is in `features/skill/components/SkillSidebarPanel.tsx`, which uses `components/sidebar/SidebarPanel.tsx`; `SidebarPanel` forwards edit handling to `components/workspace/SourceDropdownSlot.tsx`, but skill does not pass `onEditSources`, so no edit action is wired. Tool rendering is in `features/tool/components/ToolSidebarPanel.tsx`; it now uses `SidebarPanel`, but still has no source dropdown or editor path.
- [x] why can skill not pass on EditSources?
  > Response: It can pass `onEditSources`; the current blocker is that skill only has a read path (`features/skill/skill.api.ts` exposes `getSkillSearchSources`) and no editor panel/controller equivalent to address or dataset. To wire it correctly we need a skill source editor component plus a controller/tab opener, then `features/skill/components/SkillSidebarPanel.tsx` can pass `onEditSources={() => window.dispatchEvent(new CustomEvent("research-agent:edit-skill-sources"))}` into `components/sidebar/SidebarPanel.tsx`.
- [x] change this "features/tool/components/providers/builtins.ts" to sharedSkills.ts, instead of having a skill folder in data, we store the skill directly in the skill.json. since its md, we make sure the md formatting can be preserted and displayed in the browser despite saving in a json. i remember for report we had a rich text editor, make an icon which is essentially "</>", make that the icon in the ActionsMenu, and create EditorRichView. but instead of making every json field rich, we should make the rich things only apply to fields that are defined as rich in the schema. so for skill, the actual content field would have been marked as rich, the other ones are not. so the other are simpley "key inputforvalue" if its rich then "key richmenu\ntextarea" this should be navigatable between raw and rich. this also prompts us to write the update functionality so we can wire up the on Edit sources. but sources go into the search.json instead of skill.json
  > Response: Implemented in smaller pieces: skill markdown content now lives in `data/features/skill.json`; `features/skill/skill.schema.ts` marks only `content` as rich; `components/editor/EditorRichView.tsx` provides raw/rich navigation and saving; `features/skill/server/*` and `features/skill/skill.api.ts` save updated skills; skill source editing opens from `features/skill/components/SkillSidebarPanel.tsx` and saves search sources through `data/search.json`; app-owned skills are represented by `features/skill/providers/sharedSkills.ts`; tool builtins moved to `features/tool/components/providers/sharedTools.ts`.
- [x] we want to get rid of domslot, we want everything to be react components, would we encounter any difficulty or disruption to our already organized codespace? if so, can you still try safely to reduce DomSlot's use?
  > Response: Full removal would be disruptive right now because address, dataset, catalog, and agent editor panels still build large editable DOM trees imperatively and then mount small React wrappers around them. Safely reduced current use by moving action-menu DOM-node mounting into `components/editor/EditorActionsMenu.tsx`; the remaining direct `DomSlot` use is `features/dataset/components/CatalogPanel.jsx`, where an imperative catalog list is hosted inside `EditorListView`.
- [x] we can significantly reduce catalogpanel which is basically it is essentially the list view with edit functionality. also we want to move towards agent centric, so instead of storing every variable, we can just write a paragraph for each describing what we want and how to use this dataset. for now dont need to change the data stored, but keep this in mind, maybe it will help us understand what is necessary to preserve vs what can be destroyed during refactor.
  > Response: Noted for the refactor direction. The practical preservation boundary is catalog identity, provider type, URL/base URL, auth/cost metadata, and whatever fields are required to execute search. Descriptive usage guidance can become the agent-facing layer later without changing stored dataset records in this pass.
- [x] Maybe it'd be more beneficial if we have a general EditorPanel that hosts the ActionsMenu, and the ActionsMenu would determine which View they call.
  > Response: Recommended, but this should be a separate refactor. The current pages mix React-rendered editor views with imperative DOM panels, so a general `EditorPanel` should first define a clear contract for menu actions, view switching, and legacy `DomSlot` content before moving existing pages into it.
- [x] for dataset, also rename Tab to SidebarPanel, and remove the magnifying glass icon button from there, also remove the attach to context. remove the single click to show variables. basically strip it down to be able to use the new SidebarCard we have. and when double clicking also open the raw page which shows that dataset record in data/xx.json
- [x] i know we have not published yet, but can you start maintaining a changelog since we already have versions. for past versions try to explore the git history if it tells anything, if not its fine just start from now on.
  > Response: Added `CHANGELOG.md` with recovered entries from the `lib/appVersion.ts` bump history and recent commit messages covering `v0.0.1` through `v0.0.6`.
- [x] for the RawView, we want wrapped text to align with the start of the value in the first line, we could make it a div for the '"key": ' then another for the value, place them side by side possibly in a flex, so the subsequent lines of the wrapped text dont go to the beginning of line.
  > Response: Implemented in `components/editor/EditorRawView.tsx`; raw JSON now renders each line with separate indent, key, and value spans. The wrapping alignment styles are in `app/layout.css` under `.editor-raw-view__line`, `__indent`, `__key`, and `__value`.
- [x] we want to organize the editor area like this: EditorPanel {EditorNavbar {EditorNavItem, ...}, EditorPanelItem {EditorActionsMenu, EditorxxxView}}
  > Response: Added the editor shell in `components/editor/EditorPanel.tsx`, `components/editor/EditorNavbar.tsx`, and `components/editor/EditorPanelItem.tsx`. `components/Application.jsx` now uses those components while preserving the existing `editorTabBar` and `editorViewport` IDs for `components/editor/EditorTabs.js`.
- [x] we also want to get rid of the "view-menu-toolbar". if there were action for the edit.svg, we want to link it to double click action on the feature name such as SKILL, MAP, etc.
  > Response: Removed the toolbar markup from `components/Application.jsx` and deleted the toolbar styles from `components/workspace/workspace.css`. Feature-name double-click now originates in `components/sidebar/SidebarPanel.tsx` and routes through `components/initializeMapApp.js`; dataset and agent editor entry points now listen in `features/dataset/components/DatasetSidebarPanel.tsx` and `features/agent/components/AgentTab.jsx`.
- [x] we also want to move the "Sidebar header" to cover the sidebar nav menu too (be on top of it, not the nav bar on the left of it). so the structure becomes SidebarPanel {SidebarHeader, {SidebarNavbar {SidebarNavItem, ...}, SidebarPanelItem}}. it is possible that SidebarPanel and EditorPanel are instead reusable components that are imported by instances. but i think we do need an overall wrapper for these two secitons, so the app/page.tsx can import and use them directly. so if you propose a different name for reusable components vs singletons, let me know.
  > Response: Updated the shell structure in `components/Application.jsx` so `SidebarHeader` sits above both `SidebarNavbar` and the active sidebar content. Layout support is in `app/layout.css` with `.workspace-sidebar-body` and `.workspace-sidebar-panel-item`. I kept the current names for now: `SidebarPanel` remains the reusable feature panel, and `workspace-sidebar-panel-item` is only the layout slot.
- [x] we want to collapse app/page.tsx, WorkspaceClient, and Application.jsx into one. and as mentioned before, some logic in application.jsx might be distilled separately into SidebarPanel, and EditorPanel, if we choose to name them like this. maybe for the reusable components we name them SidebarPanelItem EditorPanelItem.
  > Response: I do not recommend collapsing all three in this pass. `app/page.tsx` is a server component entry, `components/workspace/WorkspaceClient.tsx` gates external MapLibre/Mapbox script readiness, and `components/Application.jsx` still owns a large client-only controller bridge. The safer next step is to keep `page.tsx` thin, eventually move script readiness into the app shell, and continue distilling `Application.jsx` into `SidebarPanel`/`EditorPanel` wrappers plus smaller controller hooks.
- [x] for dataset, remove the magnifying glass buttons on the side, we want to remove manual calling from now on.
  > Response: Removed the compact dataset manual run button and its `runDatasetSource` path from `features/dataset/components/DatasetSidebarPanel.tsx`. Removed the `.source-run-button` styles from `features/dataset/dataset.css`.
- [x] for settings, if there is no such record in the localstorage, we should initialized it. write below in which file(s) you implemented the logic.
  > Response: Implemented in `components/Application.jsx`. `getStoredSettings()` initializes `research-agent.settings` in `localStorage` with `DEFAULT_APP_SETTINGS` before opening the Settings raw editor.
- [x] after all the above changes, look for any component that are not used, or logic that are not wired, and list them below.
  > Response: Audit results:
  > - `components/editor/EditorGraphView.tsx` appears unused; `rg` only finds its own export.
  > - `components/editor/DomSlot.tsx` is still wired, but only as legacy support through `components/editor/EditorActionsMenu.tsx` and `features/dataset/components/CatalogPanel.jsx`.
  > - `components/editor/EditorTabs.js` is still the imperative editor tab/controller layer behind the new `EditorPanel` shell.
  > - `features/dataset/components/CatalogPanel.jsx`, `features/address/components/AddressTab.tsx`, `features/folder/components/FolderTab.tsx`, and `features/agent/components/AgentTab.jsx` still create substantial editor UI imperatively.
  > - Removed stale toolbar/manual-run references for `view-menu-toolbar`, `editFeatureButton`, `editDatasetButton`, `editSearchSourcesButton`, `editAgentButton`, `layerSourcesButton`, `wrapJsonTextButton`, and `source-run-button`; targeted `rg` search no longer finds those symbols.
- [x] create a primitives folder in components. and move Search Widget in there. Search Widget should be a component not a ts. its css should be in the one single Primitives.module.css inside the primitives folder. and remove the search folder. Source Dropdown should also be in primitive.
- [ ] we want to remove the entire agent customization. so agent feature, instead of allowing adding/customizing agents, we will be managing sessions. so now in the agent.json we will store things like sessions id, history. and we can also get rid of the inter agent communication tools such as list_agents, call_agent, etc.
- [ ] also completely remove the "blind" feature.
- [ ] in the sidebar header we now have the workspace selector, but move it to the workspace sidebar panel, the selector should be where the feature name for other features' sidebar panel is.
